@{
    ViewData["Title"] = "Password Reimpostata";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card border-0 shadow-lg">
                <div class="card-header bg-success text-white text-center py-4">
                    <h4 class="mb-0">
                        <i class="bi bi-check-circle me-2"></i>Password Reimpostata
                    </h4>
                </div>
                <div class="card-body p-4 text-center">
                    <div class="mb-4">
                        <i class="bi bi-shield-check display-1 text-success"></i>
                    </div>
                    
                    <h5 class="mb-3">Operazione Completata!</h5>
                    
                    <p class="text-muted mb-4">
                        La tua password è stata reimpostata con successo. 
                        Ora puoi accedere con la nuova password.
                    </p>

                    <div class="d-grid">
                        <a asp-action="Login" class="btn btn-primary btn-lg">
                            <i class="bi bi-box-arrow-in-right me-2"></i>Vai al Login
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
